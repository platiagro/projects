import routes from './routes';
import controller from './controller';
import model from './model';

export const ComponentsRoutes = routes;
export const ComponentsController = controller;
export const ComponentsModel = model;
