import routes from './routes';
import controller from './controller';
import model from './model';

export const ExperimentComponentsRoutes = routes;
export const ExperimentComponentsController = controller;
export const ExperimentComponentsModel = model;
